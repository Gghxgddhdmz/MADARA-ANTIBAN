-- حماية متقدمة ضد الباند - 99%
if game.CoreGui:FindFirstChild("AdvancedProtectionGUI") then
	game.CoreGui:FindFirstChild("AdvancedProtectionGUI"):Destroy()
end

local sg = Instance.new("ScreenGui", game.CoreGui)
sg.Name = "AdvancedProtectionGUI"
sg.ResetOnSpawn = false

local f = Instance.new("Frame", sg)
f.Size = UDim2.new(0, 300, 0, 160)
f.Position = UDim2.new(0.35, 0, 0.35, 0)
f.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
f.Active = true
f.Draggable = true
Instance.new("UICorner", f).CornerRadius = UDim.new(0, 8)

local x = Instance.new("TextButton", f)
x.Size = UDim2.new(0, 30, 0, 30)
x.Position = UDim2.new(1, -35, 0, 5)
x.Text = "X"
x.BackgroundColor3 = Color3.fromRGB(120, 0, 0)
x.TextColor3 = Color3.fromRGB(255, 255, 255)
x.Font = Enum.Font.SourceSansBold
x.TextSize = 18
Instance.new("UICorner", x)

local btn = Instance.new("TextButton", f)
btn.Size = UDim2.new(0, 220, 0, 50)
btn.Position = UDim2.new(0.5, -110, 0.5, -10)
btn.Text = "تفعيل الحماية المتقدمة"
btn.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
btn.TextColor3 = Color3.fromRGB(255, 255, 255)
btn.Font = Enum.Font.SourceSansBold
btn.TextSize = 16
Instance.new("UICorner", btn)

local restore = Instance.new("TextButton", sg)
restore.Size = UDim2.new(0, 130, 0, 30)
restore.Position = UDim2.new(1, -140, 1, -40)
restore.Text = "استرجاع الحماية"
restore.Visible = false
restore.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
restore.TextColor3 = Color3.fromRGB(255, 255, 255)
restore.Font = Enum.Font.SourceSans
restore.TextSize = 16
Instance.new("UICorner", restore)

x.MouseButton1Click:Connect(function()
	f.Visible = false
	restore.Visible = true
end)

restore.MouseButton1Click:Connect(function()
	f.Visible = true
	restore.Visible = false
end)

btn.MouseButton1Click:Connect(function()
	btn.Text = "جاري تفعيل الحماية..."
	pcall(function()
		local plr = game:GetService("Players").LocalPlayer
		local char = plr.Character or plr.CharacterAdded:Wait()

		-- إخفاء الاسم
		local h = char:FindFirstChild("Humanoid")
		if h then h.NameDisplayDistance = 0 end

		-- تعطيل الطرد بسبب عدم الحركة
		for _, v in pairs(getconnections(plr.Idled)) do
			v:Disable()
		end

		-- تعطيل kick / Ban / Shutdown
		for _, v in pairs(getgc(true)) do
			if typeof(v) == "function" and islclosure(v) then
				local info = debug.getinfo(v)
				if info.name == "kick" or info.name == "Ban" or info.name == "Shutdown" then
					hookfunction(v, function() return end)
				end
			end
		end

		-- تعطيل الريموتات المشبوهة
		for _, obj in pairs(game:GetDescendants()) do
			if obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction") then
				local name = obj.Name:lower()
				if name:find("ban") or name:find("report") or name:find("cheat") then
					obj:Destroy()
				end
			end
		end

		-- منع destroy لـ LocalPlayer أو CoreGui
		local mt = getrawmetatable(game)
		setreadonly(mt, false)
		local old = mt.__namecall

		mt.__namecall = newcclosure(function(self, ...)
			local method = getnamecallmethod()
			if (method == "Kick" or method == "Destroy") and (self == plr or tostring(self):lower():find("coregui")) then
				return nil
			end
			return old(self, ...)
		end)
	end)

	btn.Text = "تم تفعيل الحماية المتقدمة!"
	btn.BackgroundColor3 = Color3.fromRGB(40, 120, 40)
end)
